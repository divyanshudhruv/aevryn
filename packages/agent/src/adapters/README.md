NEWTODO these adapters's schema will be available in a toolcall, LLM can invoke it to find for schema (or the adapters schema will be present as a memory GLOBALLY, so all workflow agents can use it, you decide what)

NEWTODO i think they are present in capabilities, right. if yes then no need to add it again